[package]
name = "lunaris-modules"
version = "0.1.0"
edition = "2021"
description = "Module manifest parser and validation for Lunaris OS"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
